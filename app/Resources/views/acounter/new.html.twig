{% extends 'base.html.twig' %}

{% block body %}

    <h1>Acounter creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create"  style="background-color:#ffcc00; width: 100px;height: 50px " />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('acounter_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
